package G1.level_up.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Este archivo define la paleta de colores de la aplicación.
 * Aunque el proyecto utiliza una paleta personalizada definida en `login.kt`
 * (PrimaryDark, PrimaryAccent, etc.), estos colores son generados por defecto por Android Studio
 * y se mantienen aquí como referencia o para un futuro sistema de temas más avanzado (claro/oscuro).
 */

// Paleta de colores para el tema claro (Light Theme)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

// Paleta de colores para el tema oscuro (Dark Theme)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
